# v0.4.2-macos-v1-port：Mac V1 真实数据与平台适配

> 状态：进行中（Mac 原生实时 1/100/300/800、全市场七批和最终修复后的双次手动真实
> Top3 已通过；最终 arm64 `.app` 已构建、安装并完成 Keychain/SQLite/PDF/单实例/退出验收；
> 7月31日真实静态收盘回顾已盘后补生成。Primary 普通 Pro 静态路线仍限流，新鲜09:45或
> 14:45固定 Top3与交易日15:30准点生成仍待下一交易窗口）
>
> 创建：2026-07-30
>
> 冻结业务基线：`5b20b707e83baa16b1486894f8e53f343830d67c`
>
> 共享连接返修：`74b4840d25766097a2c88e502983b375bc80c7d6`、
> `d1bef74297722535fa6c96911672f473ea5f477a`、
> `21ad028541e2b40ae72b7152a865b080983a9fb4`、
> `1cb9d2a2e702c609dd76b357fa4622cf869c72b7`、
> `4e1d29bae474f284fb7b814219b4c3e1494c8dfa`
>
> 共享产品增量：`6309377187d26c46f1994ce201c16693a3caca75`
> （盘中手动 Top3、自动固定提醒回归、15:30 全市场盘后回顾；可后续独立同步 Windows）
>
> 工作分支：`feat/macos-v1-port`

## 决策与证据边界

Human Owner 于 2026-07-30 决定 Mac 先行：在 macOS 上使用与 Windows 相同的 Tushare
核心路线、共享候选逻辑和锁定产品规则，先完成连接门返修、真实全市场 Top3 和桌面验证，
然后把**共享提交**同步给 Windows。

这不改变 Windows 真实验收事实：冻结 Windows 基线在统一 Token 连接校验阶段收到
`rate_limited`，完整实时扫描轮次和真实 Top3 均为 0，结论继续为 `FAIL`。本版本任何
Mac 数据、截图、`.app` 或测试都不能作为 Windows 通过、Windows 通知、Windows 安装或
TdxQuant/M0 通过证据。

## 范围

### 已实施的 Mac 专属层

- [x] 数据/配置使用 `~/Library/Application Support/StockWatcher`，日志使用
  `~/Library/Logs/StockWatcher`，报告保留在 Application Support 的 `reports/`。
- [x] macOS 仅接受实际 `keyring.backends.macOS` Keychain backend；设置页显示
  “系统钥匙串”，并且 Token 不进入 YAML、SQLite、日志、源码、截图或 Git。
- [x] 主窗口关闭时隐藏并继续扫描；Dock/应用激活恢复窗口；应用菜单提供“显示主窗口”、
  “数据接口”和“退出 StockWatcher”。
- [x] 使用 `QLocalServer/QLocalSocket` 保证单实例，第二个进程只请求唤起已有窗口。
- [x] 保留 Qt 右下角三行弹窗；Notification Center 仅为次要 best-effort 通知，权限拒绝
  或 Focus 不影响应用内弹窗；默认优先主窗口所在显示器右下角。
- [x] 睡眠/唤醒和网络恢复清除滚动基线、取消在途扫描并进入 WARMING；连续三轮新鲜完整
  数据前不形成新候选。网络断开时停止定时扫描，保留旧三只但不产生新结果。
- [x] 默认 Mac 入口延迟加载 TdxQuant 诊断代码，避免普通 Tushare Mac 进程加载 Windows
  Tdx 依赖；Windows PowerShell/VBS/Inno/TdxQuant 文件仍保留，后续 Mac 打包必须排除。
- [x] 内部测试入口：`scripts/install_macos_dev_app.py` 可生成
  `~/Applications/StockWatcher Dev.app`，使用 macOS 留白图标并从当前工程启动真实 UI，方便
  Human Owner 打开“数据接口”手动输入 Token；它不是最终 PyInstaller 发布包。
- [x] 主界面布局调整：压缩顶部状态和连接说明区，把候选列表/空候选占位作为主视觉区域，
  避免接口状态抢占 3 只观察股票的位置。
- [x] 主界面提供醒目的“立即获取最新3只”：盘中点击后强制执行一次最新全市场扫描，
  成功即更新主界面并弹出同一组三只；手动查看不写自动提醒事件、不占用 09:45、14:45
  或强异动每日限额，数据失败时保留上次结果且不产生新弹窗。
- [x] 09:45、14:45 继续由交易时段调度器自动触发；扫描跨越固定时点时补触发，同一分钟
  和应用重启后均按当日持久化记录去重。
- [x] 15:30 自动生成全市场盘后回顾：包含涨跌家数、上涨比例、中位涨幅、强势行业、
  收盘观察 Top3、自动提醒次数、资金与数据限制；手动查看不计入提醒限额。结果写入 SQLite
  总结页，并原子生成
  `reports/YYYY-MM-DD-A股盘后回顾.json`、`.md` 与固定 A4 纵向三页 `.pdf`。普通菜单
  “盘后回顾与PDF”可选择今天或最近31个自然日的历史报告，并通过 macOS 保存对话框下载；
  内部报告与 SQLite 总结超出31日自动清理，用户已下载的外部副本不受影响。
- [x] PDF 使用本地确定性脚本排版，不接 AI 大模型、不向外部服务上传行情、候选或凭据；
  固定三页分别为市场全景、收盘观察 Top3、运行回顾与数据说明。Human Owner 于
  2026-07-31 接受当前研究简报排版并锁定为 `research-brief-v1`；日常导出只替换日期与数据，
  后续视觉改版必须显式确认并提升模板版本。
- [x] 历史分钟继续独立显示能力状态，但不可用时不再阻塞核心实时 1/100/300/800
  渐进检查、全市场扫描和 Top3；资金未知同样不阻塞。
- [x] 股票名单、板块、已完成交易日趋势和资金能力写入带 SHA-256 校验的原子运行缓存；
  09:45、14:45、手动和连续扫描只读取已验证缓存，不在实时扫描里调用普通 Pro。缓存缺失、
  损坏或过期时停止新候选，后台刷新失败保留旧缓存。
- [x] 普通 Pro 与原生实时继续共用一个应用级请求起点预算；429 冷却按路线隔离，因此 Pro
  限流不再遮蔽 `realtime_quote(src="sina")`。应用在全市场七批之前只后台执行
  1/100/300/800 实时递进，不在交易窗口启动普通 Pro 能力检查。

### 仍待真实环境验证

- [x] Primary 凭据已通过 macOS “系统钥匙串”保存；不进入命令、配置、日志、截图或 Git。
- [ ] 非交易时段：轻量保存、静态/历史/板块及单只原生实时结构。
- [x] 交易时段：1/100/300/800 批次递进、连续全市场扫描、覆盖率、source age、价格量额
  推进、429、错误、p50/p95 和恢复三轮指标。
- [x] 真实 Top3：板块硬门、同板块最多两只、强/中/近、补位观察、稳定替换、资金未确认
  不阻塞。
- [x] 14:45 固定弹窗真实触发并落库；当时因前台切换误触发恢复门，弹窗正确使用上一轮三只
  并明确显示数据延迟，因此只证明固定触发与延迟兜底，不证明新鲜固定 Top3。自然强异动已在
  14:28、14:31 和 14:55 三次真实落库。
- [x] 7月31日真实静态收盘回顾已盘后补生成并进入 SQLite、JSON、Markdown和固定三页PDF；
  明确标记`RETROSPECTIVE_ONLY`，不能替代交易日15:30准点生成或盘中Live证据。
- [ ] 30天历史完整口径与交易日15:30准点生成证据。
- [x] Retina内屏与外接屏环境识别、外接屏实际窗口布局、关闭隐藏、第二启动唤起及显式退出。
- [ ] 真实睡眠/唤醒与断网/恢复的人工图形会话验收；状态机自动回归已通过。
- [x] 新增独立 Mac PyInstaller spec，构建本机 arm64 `.app`，完成ad-hoc签名，并在全新复制
  目录和`~/Applications/StockWatcher.app`验证Keychain、数据库、历史报告/PDF、显式退出与
  第二次启动。

## 数据与安全契约

- 普通、历史和板块固定走 `https://fastapic.stockai888.top`；主实时固定走
  `tushare.realtime_quote(..., src="sina")`，原生校验地址为
  `https://realtime.stockai888.top`。
- 普通 Pro 遵循供应商文档的 SDK 调用路径：`POST /<api_name>` 并带
  `ts_type_name`；为避免 `ts.set_token()` 写入 `~/tk.csv`，应用以 Keychain 中的 Token
  在内存中执行同一 SDK wire contract，不使用环境变量或文件兜底。
- `rt_k` 不进入 15000 积分主实时路线；资金无可靠盘中数据时显示“资金未确认”，不得以
  日级 moneyflow 冒充盘中大单/超大单，也不得阻塞 Top3。
- 应用级共享请求预算的默认请求起点间隔为 1 秒；429 保留 Token，遵守 `Retry-After`，
  无值时 60 秒冷却后从失败能力继续检查。
- 共享连接与实时返修位于 `74b4840d…`、`d1bef742…`、`21ad028…`、`1cb9d2a2…` 与
  `4e1d29ba…`；这些提交不含 Keychain、macOS 生命周期、Notification Center 或 Mac 打包
  入口。Windows 同步时仍应在独立分支按顺序回放并重跑 Windows 工程门，不能把 Mac 证据
  直接带过去。

## 当前可复现实测

- 2026-07-30：实际 backend 为 `keyring.backends.macOS.Keyring`；使用临时非敏感条目
  完成写入、读取、删除 round-trip，结果 PASS，未保留条目。
- 2026-07-30：旧的根路径普通 Pro `trade_cal` 轻量实测曾返回 `rate_limited`，凭据保留，
  未将该结果写成 Token 无效。随后按供应商文档切换 SDK 路径，在 macOS Keychain 中读取
  Primary Token 进行一次 `POST /trade_cal` 轻量实测，HTTP 200、8 条；这只证明基础连接，
  不等同于全市场扫描或交易时段验收。
- 2026-07-30：按文档原生 `realtime_quote(src="sina")` 单证券结构实测返回 HTTP 200、1 条，
  且有代码、价格、昨收、量、额和供应商时间字段；这只证明接口结构，不是交易时段全市场
  扫描或真实 Top3。
- 2026-07-30：单实例 guard 已覆盖异常中断后遗留 Unix socket 的恢复：仅在
  `ServerNotFoundError` 或 `ConnectionRefusedError` 时回收端点；可连接的主进程仍优先被
  唤起，避免第二次启动永久失败。
- 2026-08-01最终收口：`uv sync --all-groups --frozen`、`uv lock --check`、全量pytest
  （302 passed、20 skipped、2 deselected）、Ruff、Mypy（99个源文件）、workspace 29项和
  `git diff --check`全部通过；Replay五状态PNG 5/5重新生成，SQLite WAL/备份/回滚/迁移
  定向回归5/5通过。秘密扫描未发现真实Token或长不透明凭据；唯一命中为测试中的显式假值。
- 2026-07-31 13:05–13:39 CST：原生实时全市场返回5530只；独立聚合验证中
  1/100/300/800均通过，全市场按`800×6+730`完成，5510只新鲜可用、20只旧行情逐股排除，
  覆盖99.6383%，耗时18.689秒，429、失败、重叠和重复拒绝均为0。聚合证据为
  `~/Library/Application Support/StockWatcher/evidence/live-validation-20260731-1338.json`，
  SHA-256 `962d58ebdf0282523cec9916adbfd13636fdea0a8f4c1776f3d1df1e730cad9d`。
- 2026-07-31 13:18–13:55 CST：Mac App多次真实生成并持续更新三只候选。首轮为致远互联、
  博思软件、恒银科技，验证补位与同板块最多2只；后续轮出现真实“中/强”。13:55按用户实际
  路径在后台扫描期间点击“立即获取最新3只”，界面立即显示排队、三步进度和倒计时，约50秒
  内完成（实际全市场扫描约28秒），显示科德教育、捷成股份、秦川机床并恢复按钮。资金均为
  “资金未确认”，未阻塞候选。
- 2026-07-31 14:07–14:23 CST：按真实用户路径继续双击验收时发现稳定替换会在候选未进入
  新一轮Top3时保留上一轮完整对象，可能让旧`source_ts`继续显示为当前候选。该轮不计为规则
  验收通过。共享运行时已改为：稳定规则只保留席位，价格、涨幅、分数、级别、解释和时间全部
  从本轮新鲜合规输入刷新；当前轮缺失、过期或被排除的股票立即退出，不等待3轮。修复版重启后
  连续两次点击分别约31秒和35秒成功。第二次Top3为长盛轴承、米奥会展、读客文化，行情年龄
  均约12秒，板块中位涨幅为正、上涨比例均高于89%、明显走强数量均不少于14只、板块内排名
  均在前5；三只来自不同板块，均为“中”，资金未确认未阻塞。完整工程门增至299 passed。
- 2026-07-31 14:45–15:00 CST：14:45:06 固定提醒真实落库并弹出三只，但当时运行版把普通
  前台激活误判为系统唤醒，正确进入恢复门并使用带“数据延迟”的上一轮三只；因此固定触发和
  失败兜底通过，新鲜14:45 Top3仍待下一交易窗口。平台层随后改为只有真实 suspended 状态、
  网络恢复或超过20秒的单调时钟事件循环停顿才触发三轮恢复；Finder切回StockWatcher后保持
  `HEALTHY`且未清空基线。最终修复版于14:55和14:57再次真实点击两次，分别不超过47秒和
  32秒完成。两轮均为三只正式候选、三个不同板块，板块中位涨幅为正、上涨比例大于50%、
  明显走强至少3只、板块排名前5，行情年龄1–15秒，资金未确认未阻塞。第二轮米奥会展为
  “中”，汉嘉数智和长盛轴承为“强”，分级与核心分、完整度和加速度门一致。14:59后台新鲜
  轮次中米奥会展的1/3/5分钟涨速变为`+0.00%/+0.12%/+1.92%`，加速度恢复至强级允许区间，
  主界面随之显示“强”，不是14:57旧快照的展示误标。最终双次证据为
  `manual-top3-validation-20260731-1458.json`，SHA-256
  `bc2ac5df2de7c07e6256ef448bfa612b223a3d2f7750ed5dd6cb94b68bf9655a`。
  最终两轮涉及的5只股票另以最近4个交易日连续日线和今日原生实时昨收交叉核对：没有近期
  交易日缺口，实时昨收均与7月30日收盘价一致，未发现复牌或除权机械跳变；脱敏证据
  `manual-top3-exclusion-validation-20260731.json`的SHA-256为
  `dcb54e4202ebe093d94f96f565b4a41c56e321a51b642dcaaf47d749e77cfca7`。
- 2026-07-31：使用7月30日真实静态收盘数据生成固定A4纵向三页PDF；新版排版改为
  研究简报层级：第一页加入市场判断、上涨/下跌宽度条和四项核心指标，第二页用编号卡片
  突出 Top 3，第三页加入收盘结论、运行指标和编号限制清单。`pdfinfo`确认3页、A4、
  无表单/JavaScript，逐页PNG视觉检查确认中文、数字、Top3卡片和表格无截断。新版报告
  SHA-256 为 `caeccce5f5551b39867af79aa84165ba5c9bf4651ed2f54f1e068cca47f714b1`。
  App离屏实测显示日期选择器、7月30日内容和“下载 PDF”按钮完整可见。该报告仍是
  Super高级诊断生成的真实静态收盘回顾，不构成盘中Live、固定提醒或Windows验收。
- 2026-08-01：补生成7月31日真实静态收盘回顾，生成时间如实记录为
  `2026-08-01T11:53:07+08:00`，Top3为天龙集团、昆仑万维、蓝色光标。结果写入SQLite及
  `reports/2026-07-31-A股盘后回顾.{json,md,pdf}`；PDF为A4纵向3页、无表单、JavaScript或
  加密，SHA-256为`524e87556d80344c157d25c9fb588104e6a88f1b1a037f0f609dc840a6d4c395`。
  报告明确标记盘后补生成、`RETROSPECTIVE_ONLY`和Super静态高级诊断兜底，不构成15:30
  Live、盘中Top3或Windows验收。
- 2026-08-01：独立Mac spec生成166MB、本机arm64 `StockWatcher.app`，Bundle ID为
  `com.kahlilhazel.stockwatcher`，`NSHighResolutionCapable=true`，ad-hoc签名通过
  `codesign --verify --deep --strict`。严格收紧“同板块最多2只”后重新构建，最终压缩包
  `StockWatcher-macOS-arm64.app.zip`为72,081,713 bytes，SHA-256为
  `11fbfa216118aa842d752c0229c13b665025ab8a7b288183f5a47e24454df350`；包内文件名与PyInstaller
  归档扫描均未发现TdxQuant、TQ、PowerShell、VBS、Inno或Windows portable入口。
- 2026-08-01：从全新临时目录启动最终App，并安装到`~/Applications/StockWatcher.app`。
  实际读取macOS Keychain中的Primary、现有SQLite四条提醒历史、7月30/31日报和PDF；应用内
  下载的7月30日PDF与内部源文件逐字节一致。关闭主窗口后同一进程继续运行且无可见窗口；
  再次启动安装版后仍只有原PID并恢复1040×788主窗口；应用菜单显式退出后进程消失。
  本机同时连接3024×1964 Retina内屏和3840×2160外屏，最终窗口已在外屏实际显示并检查。
- 2026-07-31 09:28–09:34 CST：交易时段对 Primary 主路线和 App“立即获取最新3只”进行
  真实复核。普通 Pro 在 `stock_basic` 数据准备阶段持续返回 `rate_limited`；应用保留系统
  钥匙串 Token、禁用手动抓取按钮并停止新候选。受控记录结论为 `BLOCKED_RATE_LIMIT`，
  完整扫描0轮、候选快照0、Top3为空，等待超过默认60秒后仍未恢复。该结果证明安全停门生效，
  但不构成实时Top3通过；问题与AI大模型无关。
- 2026-07-31 10:42:12 CST：普通 Pro 仍处于独立429状态时，按批准路线从系统钥匙串读取
  Primary 并调用一次 `realtime_quote(ts_code="000001.SZ", src="sina")`。返回1条真实盘中
  数据：价格11.44、成交量116,511,112股、成交额1,328,117,351.52元、供应商源时间10:42:12，
  source age 2.807秒、质量`HEALTHY`。该证据证明实时路线可用且不再被Pro 429阻塞；它仍不是
  1/100/300/800、全市场七批、候选或Top3通过。
- 2026-07-31 10:37与10:48 CST：使用一个进程、一个1秒请求预算尝试建立完整原子静态缓存，
  两次均在第一个普通Pro `stock_basic` 请求收到`rate_limited`，分别约2.219秒和2.102秒安全
  停止；Token保留，未生成半成品缓存，未调用Super，未启动实时扫描。因本机没有来自普通Pro
  的完整股票名单/板块/三日趋势缓存，本轮不能诚实执行全市场七批或生成Top3。
- 2026-07-31 00:43 CST：当前为非交易时段，因此本轮没有把离线测试、模拟 Provider 或
  盘后日线回顾冒充实时 Top3；下一次 live 仍必须在交易时段使用系统钥匙串中的 Primary
  凭据验证。
- 2026-07-30 15:33 CST：盘后主路线复核返回 `rate_limited`；15:43–15:44 CST 允许的
  Super 静态高级诊断返回 `empty_data`。两者均未生成报告、Top3 或当日日线覆盖，不构成
  实时、主路线、盘中 M0 或 Windows 证据。
- 2026-07-30 23:38–23:58 CST：按供应商文档直接使用 Tushare 1.4.29 SDK 复核。
  `trade_cal` 成功返回 46 条并确认 7 月 30 日开市；普通 `stock_basic` 原始 HTTP 响应持续为
  429（`请求次数超限（每分钟最多120次）`）。SDK 的 `DataApi.query()` 会因
  `requests.Response` 对 429 求值为 false 而静默返回空 DataFrame，因此此前“0 行”不是收盘
  数据不存在。停止运行中的 Dev App 后，已授权 Super 静态接口成功返回 5530 条股票资料和
  最近四个交易日 5523–5528 条日线，生成真实收盘回顾 Top3：金徽酒、舍得酒业、美邦股份。
  证据见 `post-close-review-20260730-super-diagnostic.json` 和
  `2026-07-30-盘后回顾-Super静态诊断.md`；它仍只属于高级静态诊断，不构成普通主路线、
  盘中实时、固定提醒或 Windows 验收通过。
- 实时验证脚本只读取 Primary 凭据，普通 Pro 与原生实时注入同一个 1 秒应用级预算；报告逐项
  列出 1/100/300/800 能力状态、429/错误和全市场价格/量额的**聚合**推进，不保存原始市场
  行、Token 或旧 Fast 凭据。盘后脚本同样只读取 Primary（高级 Super 诊断仅在显式参数与独立
  凭据同时存在时可用，绝不构成主路线验收）。
- 离线工程门、Replay 五状态、SQLite 和全部 Mac 专属回归已在最终生命周期修复后重新完整
  通过；它们只证明 macOS 本地工程行为，不证明真实 Token、真实行情或 Windows。

## 唯一下一步

下个交易窗口在安装版App中复验新鲜09:45或14:45固定Top3，并在15:30现场确认准点报告；
同时补真实睡眠/唤醒与网络恢复的人工图形会话。Primary普通Pro静态路线继续单独标记为
`rate_limited`，不得用授权Super静态缓存、Mac实时PASS、Replay或大模型覆盖该缺口。

本轮逐项证据见 [validation-20260731.md](validation-20260731.md)。
