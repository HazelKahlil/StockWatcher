# 2026-07-31 Mac 实时路线与 2026-08-01 最终 App 验证记录

> 环境：macOS 本机、`feat/macos-v1-port`。凭据仅从实际 macOS Keychain 读取；未写入
> 命令参数、源码、配置、日志、报告、截图或 Git。未访问 GitHub，未使用 Super、`rt_k`、
> Replay 或大模型生成候选。以下结果只能证明 Mac，不代表 Windows 通过。

## 本轮固定路线

实时只允许：

```python
from tushare.stock import cons as ct

ct.verify_token_url = "https://realtime.stockai888.top"
df = ts.realtime_quote(ts_code="多只股票代码", src="sina")
```

普通 Pro 仅用于交易窗口前准备股票名单、板块和已完成交易日趋势缓存。实时窗口读取原子缓存，
按1只、100只、300只、800只递进后，再以每批最多800只、批次请求起点至少间隔1秒完成全市场
七批。`rt_k`和Super不进入候选链。

## 离线工程门

| 项目 | 结果 |
| --- | --- |
| 全量 pytest | 302 passed、20 skipped、2 deselected |
| Ruff | PASS |
| Mypy | PASS，99个源文件 |
| workspace | PASS，29个必需文件 |
| lock / diff | `uv lock --check`、`git diff --check` PASS |
| Replay | 五状态PNG 5/5生成 |
| SQLite | WAL、备份、回滚、迁移定向5/5 |

新增回归覆盖：

- 5500只严格拆为`800×6 + 700`；
- SDK真实形态HTTP 429保留为`rate_limited`，不被装饰器改写；
- Pro与实时共享请求起点间隔，但Pro 429不冻结实时路线；
- 交易窗口只后台执行1/100/300/800，不调用普通Pro能力检查；
- 原子缓存SHA-256、完整性、时效、失败替换保留旧版本；
- 缓存缺失、损坏或过期时停止新候选。
- 5530只中少量停牌或久未成交证券只逐股排除；可用于候选的新鲜覆盖仍须不低于99%；
- 旧时间、缺时间和不可用行不进入滚动基线，供应商`source_ts`不被接收时间改写；
- 相同`code + source_ts`且供应商字段未变化时，忽略新`scan_id/received_ts`信封差异，
  不把安静个股误判为整轮时间冲突；真实字段变化仍拒绝；
- Qt工作线程完全退出后才重建候选卡片，避免结果出现后界面死锁；
- 后台连续扫描占用数据通道时，“立即获取”保持可点并排队，当前轮结束后自动执行。
- 稳定替换只保留候选席位，不保留旧行情对象：保留期间的价格、涨幅、分数、级别、原因与
  `source_ts`必须来自当前新鲜合规扫描；当前轮缺失、过期或被排除的证券立即退出，不等待3轮；
  刷新后仍重新执行同板块最多2只的显示约束。
- macOS普通前后台切换不得冒充睡眠/唤醒；只有真实suspended状态、网络恢复或超过20秒的
  单调时钟事件循环停顿才进入三轮恢复。

## 真实接口结果

| 时间（CST） | 操作 | 结果 | 严格结论 |
| --- | --- | --- | --- |
| 10:37 | 单进程生成原子静态缓存 | `stock_basic`为`rate_limited`，2.219秒停止 | 缓存未生成；Token保留 |
| 10:42:12 | 单只原生实时 | 1条；价格11.44；量116,511,112股；额1,328,117,351.52元；source age 2.807秒；`HEALTHY` | 实时路线真实可用，且未被Pro 429阻塞 |
| 10:48 | 再次单进程生成原子静态缓存 | `stock_basic`为`rate_limited`，2.102秒停止 | 缓存仍未生成；停止重试 |
| 12:12 | 生成原子静态缓存 | 授权的Super静态兜底取得5530只股票、5524条行业关系和截至7月30日的趋势 | 只解决本次Mac实时验证的静态上下文；**不代表Primary普通Pro通过** |
| 13:05 | 原生实时全市场诊断 | 请求/返回5530只，20.535秒；5524只60秒内，6只旧成交时间；source age p50 3.532秒、p95 11.494秒 | 接口真实可用；旧时间不得放大为整轮失败 |
| 13:18 | App“立即获取最新3只” | 约30秒显示致远互联、博思软件、恒银科技；两只软件服务、一只IT设备；均为“近｜补位观察” | 首轮真实Top3 PASS；同板块最多2只、资金未确认不阻塞 |
| 13:33–13:35 | 修复版App手动与连续扫描 | 手动约30秒完成且按钮恢复；后续轮形成分钟基线，真实出现“中”候选并持续更新 | Qt界面可继续操作；安静个股重复源时间不再拖垮整轮 |
| 13:38–13:39 | 独立聚合验证 | 1/100/300/800均通过；全市场七批`800×6+730`；5530返回、5510新鲜可用、20只旧行情排除；覆盖99.6383%，18.689秒，最大source age 57.067秒，source span 55秒 | `PASS`；0次429、0重复拒绝、0重叠、0失败 |
| 13:43–13:44 | 后台扫描中再次点击“立即获取” | 按钮显示“已排队，当前扫描结束后获取…”，约28秒后自动完成并重新可点；Top3为迈信林、省广集团、天铁科技，均为“中” | 用户操作不再因连续后台扫描而失效 |
| 13:54–13:55 | 按用户实际路径再次点击“立即获取最新3只”并计时 | 点击后立即显示排队；随后显示第3/3步、全市场1/1轮和60秒倒计时；约50秒内完成，其中实际全市场扫描约28秒；Top3为科德教育、捷成股份、秦川机床，均为“强” | 真实手动路径在一分钟内完成；按钮恢复可点，SQLite原子保存3只 |
| 14:07–14:10 | 再连续执行两次用户路径并审计SQLite候选原始字段 | 两次都成功产生新快照，但第二次的稳定名单仍携带上一轮候选对象，其中一只`source_ts`早于本轮数分钟 | **规则审计FAIL**；抓取成功不等于“当下Top3”，不得作为最终通过 |
| 14:19–14:20 | 修复并重启后第一次点击 | 约31秒完成；致远互联、恒银科技、博思软件，均为“近｜补位观察”，顶部“本轮整体偏弱”；行情年龄约4–14秒 | PASS；重启首轮无分钟基线，按规则不得冒充“中/强”；软件服务最多2只 |
| 14:20–14:21 | 修复后第二次点击 | 约35秒完成；长盛轴承、米奥会展、读客文化，均为“中”；行情年龄约12秒；三个板块中位涨幅3.9053%/2.6439%/1.8836%，上涨比例93.43%/90.20%/89.66%，明显走强103/34/14只，板块排名4/137、1/51、3/29 | PASS；三只全部通过板块硬门且位于各板块前5，来自三个不同板块；资金未确认未阻塞 |
| 14:45:06 | 固定提醒 | `scheduled-14:45`事件落库并弹出；因当时前台激活误触发恢复门，使用14:44上一轮三只并明确显示数据延迟 | 固定触发与延迟兜底PASS；**新鲜14:45 Top3未证明** |
| 14:54 | Finder切回修复版App | 保持`HEALTHY`，未清空实时基线 | 普通前后台切换不再误判系统唤醒 |
| 14:55–14:56 | 最终修复后第一次点击 | 后台扫描占用时自动排队；不超过47秒完成；迈信林、米奥会展、安记食品，均为“中”，行情年龄1–15秒 | PASS；三只均为正式候选、三个不同板块，全部通过板块硬门且在板块前5 |
| 14:57:10–14:57:42 | 最终修复后第二次点击 | 约32秒完成；米奥会展“中”、汉嘉数智“强”、长盛轴承“强”，行情年龄约13秒 | PASS；三只均通过板块硬门；强中分级与核心分、完整度和加速度门一致 |
| 14:59 | 后台新鲜轮次 | 米奥会展1/3/5分钟涨速变为`+0.00%/+0.12%/+1.92%`，主界面级别更新为“强” | 后台结果晚于14:57持久快照；级别变化来自新鲜滚动特征，不是展示误标 |

13:38聚合报告仅保存能力状态、全市场聚合指标和Top3，不保存原始5530行行情：

- 路径：`~/Library/Application Support/StockWatcher/evidence/live-validation-20260731-1338.json`
- SHA-256：`962d58ebdf0282523cec9916adbfd13636fdea0a8f4c1776f3d1df1e730cad9d`
- 凭据存储：系统钥匙串；报告明确`credential_persisted_or_printed=false`。
- 原始行情：报告明确`raw_market_payload_persisted=false`。

14:19–14:21修复后双次手动验证报告同样不保存Token或原始全市场行情：

- 路径：`~/Library/Application Support/StockWatcher/evidence/manual-top3-validation-20260731-1421.json`
- SHA-256：`d2461b718331c0a33bff1c81390768adb36da01d22aaf104c5a76fc2eccd83ed`
- 内容：两次用户路径计时、SQLite快照ID、三只候选的板块门审计字段、规则结论、离线门与
  Primary/Windows边界。

14:55–14:57最终生命周期修复后的双次手动验证报告：

- 路径：`~/Library/Application Support/StockWatcher/evidence/manual-top3-validation-20260731-1458.json`
- SHA-256：`bc2ac5df2de7c07e6256ef448bfa612b223a3d2f7750ed5dd6cb94b68bf9655a`
- 内容：两次用户路径计时、SQLite快照14/15、板块硬门和强中分级审计、14:45严格边界、
  三次自然强异动、生命周期修复与凭据/Windows边界。

最终两轮5只唯一候选的锁定排除项交叉核对：

- 路径：
  `~/Library/Application Support/StockWatcher/evidence/manual-top3-exclusion-validation-20260731.json`
- SHA-256：`dcb54e4202ebe093d94f96f565b4a41c56e321a51b642dcaaf47d749e77cfca7`
- 结果：5只在7月27日至30日均有连续日线；7月31日原生实时昨收均与7月30日收盘价一致，
  没有近期停牌复牌缺口或除权调整昨收命中。只保存代码和结论，不保存原始市场响应或凭据。

## 2026-08-01 最终收口

### 7月31日收盘回顾

- 2026-08-01 11:53:07 CST 使用真实静态收盘数据盘后补生成7月31日报告；Top3为天龙集团、
  昆仑万维、蓝色光标。
- SQLite、JSON、Markdown和固定A4纵向三页PDF均生成成功；PDF SHA-256为
  `524e87556d80344c157d25c9fb588104e6a88f1b1a037f0f609dc840a6d4c395`。
- 报告明确标记`RETROSPECTIVE_ONLY`和Super静态高级诊断兜底，只证明盘后确定性分析与
  PDF呈现，不构成交易日15:30准点生成、盘中Live、主路线普通Pro或Windows验收证据。

### 最终离线工程门

| 项目 | 最终结果 |
| --- | --- |
| 依赖与锁文件 | `uv sync --all-groups --frozen`、`uv lock --check` PASS |
| 全量 pytest | 302 passed、20 skipped、2 deselected |
| Ruff | PASS |
| Mypy | PASS，99个源文件 |
| workspace | PASS，29项 |
| diff | `git diff --check` PASS |
| Replay | 五状态PNG 5/5重新生成 |
| SQLite | WAL、备份、回滚、迁移定向5/5 |

秘密扫描未发现真实Token或长不透明凭据；唯一规则命中来自测试中的显式假值。离线结果只
证明macOS本地工程行为，不能替代真实交易窗口或Windows证据。

### `.app`、安装与图形会话

- 独立Mac PyInstaller spec生成166MB、本机arm64 `StockWatcher.app`，Bundle ID为
  `com.kahlilhazel.stockwatcher`，`NSHighResolutionCapable=true`；ad-hoc签名通过
  `codesign --verify --deep --strict`。
- 严格收紧“同板块最多2只”后重新构建；最终压缩包
  `StockWatcher-macOS-arm64.app.zip`为72,081,713 bytes，SHA-256为
  `11fbfa216118aa842d752c0229c13b665025ab8a7b288183f5a47e24454df350`。
- 包内文件名和PyInstaller归档均未发现TdxQuant、TQ、PowerShell、VBS、Inno或Windows
  portable入口；Token、数据库、报告、缓存和日志均不进入包。
- 从全新临时目录启动后安装到`~/Applications/StockWatcher.app`。安装版实际读取macOS
  Keychain中的Primary、现有SQLite四条提醒历史以及7月30/31日报和PDF，界面未显示凭据值。
- 应用内下载的7月30日PDF与内部源文件逐字节一致；最终截图覆盖主窗口、数据接口、盘后回顾
  和历史记录，输入框、按钮、状态、候选主区域及下载入口完整可见。
- 关闭主窗口后同一进程继续运行且无可见窗口；再次启动安装版仍只有原PID并恢复1040×788
  主窗口；应用菜单显式退出后进程消失。
- 本机同时连接3024×1964 Retina内屏和3840×2160外屏；最终窗口已在外屏实际显示检查。
- 最终规则锁定重建版再次安装后，第一次启动为单进程，第二次启动保持同一PID且进程总数仍为
  1；随后正常退出。`05-final-sector-lock-main.png`来自该最终哈希安装版，Retina截图为
  2080×1576，界面无截断且未显示Token值。
- 真实睡眠/唤醒与断网/恢复尚未在人工图形会话执行；三轮WARMING状态机只有自动回归证据。

## 当前验收结论

- 原生实时1/100/300/800与全市场七批：Mac真实`PASS`。
- 修复版连续两次手动60秒内真实Top3、首轮补位、同板块最多2只、资金未确认不阻塞：
  Mac真实`PASS`。
- 连续扫描建立分钟基线并出现“中/强”、稳定结果继续更新：Mac真实观察到。
- 稳定候选字段新鲜度：新增单元与运行时集成回归，并由修复后双次真实点击确认候选字段均来自
  当前轮；先前14:07–14:10抓取虽成功但因旧对象问题明确作废，不计入通过证据。
- 数据失败时保留旧结果、恢复门与重复源时间处理：自动测试通过；真实连续轮不再因少量旧行
  或相同源时间停止。
- 最终生命周期修复后的两次手动结果均在60秒内完成并通过规则审计；14:55和14:57两轮作为
  最终双次验收证据，14:19和14:20两轮保留为稳定替换修复证据。
- Primary普通Pro静态路线仍为`rate_limited`；本次静态缓存来自授权Super兜底，因此不能写成
  Primary普通/历史/板块完整通过，也不能把Super写成实时路线。
- 14:45固定触发与延迟兜底通过，但新鲜09:45或14:45固定Top3未证明；交易日15:30准点
  报告和完整30天历史口径仍待现场验证。
- 最终arm64 `.app`构建、签名、全新目录启动、安装、Keychain/SQLite/PDF读取、关闭隐藏、
  单实例唤起、显式退出、Retina与外接屏验收：Mac真实`PASS`。
- Windows继续为`FAIL`；Mac结果不得改写Windows完整扫描0轮和真实Top3为0的冻结事实。

## 唯一下一步

下个交易窗口在安装版App中复验新鲜09:45或14:45固定Top3，并在15:30现场确认准点报告；
同时补真实睡眠/唤醒与断网/恢复的人工图形会话。Primary普通Pro静态路线继续单独标记为
`rate_limited`，不得用授权Super静态缓存、Mac实时PASS、Replay或大模型覆盖该缺口。
