# Web 内部试用版 — 18 项基线与架构确认

实施人：StockWatcher Agent（2026-08-07）
唯一实施指令：交接包 `16-Agent-Master-Prompt.md`
唯一源码基线：`baseline/StockWatcher-502a447-main-and-rc3.bundle`（commit `502a447d7e593d638ea45518f2a5e4d4827f683f`，tag `mac-v1-reliability-rc3-20260806`）
工作分支：`web/internal-test-v1-handoff`（从本地权威仓库 `main@502a447` 创建，等价于 bundle 克隆；包内 `verify-package.sh` 已证明 bundle 与 source archive 的树与 commit 一致）

| # | 已核验事实 | 证据 | 设计决定/下一动作 |
|---:|---|---|---|
| 1 | 交付包完整性：SHA256SUMS 94/94 OK；10 个 shell 脚本 `bash -n` 全过；`validate_contracts.py` PASS（OpenAPI 23 paths/29 schemas/137 refs，compose 服务恰为 caddy/web/worker）；`scan_handoff_secrets.py` 0 命中/92 文件 | `sha256sum -c SHA256SUMS.txt`（94×OK，0 失败）；`bash -n scripts/*.sh`；`python3 tests/validate_contracts.py`；`python3 tests/scan_handoff_secrets.py` | 包完整无缺失/篡改，作为唯一输入继续 |
| 2 | Git 基线：`git bundle verify` = "is okay / complete history / sha1"；clone `-b main` 后 HEAD=`502a447d...`；tag 解引用同一 commit；工作树干净；source archive 与 `git archive HEAD` 逐文件等价且无 `.git` | `verify-package.sh` 全流程（bundle verify、clone、rev-parse、status、archive diff）；本地权威仓库 `main` HEAD 与 bundle 完全一致 | 在本地权威仓库建新分支 `web/internal-test-v1-handoff`，不从任何旧分支/旧代码开始 |
| 3 | provenance：`SOURCE_COMMIT` 与 main 一致；schema v6（证据文件确认）；原 349 passed/20 skipped/2 deselected 仅作历史日志，本环境不虚报；legacy `raw-top20`=Raw Top3（415×3=1245 行，源为 `audit.raw_codes`）；`raw-top20-reconstructed`=真 Top20（415×20=8300 行，由 candidate-audit 确定性重建）；基线 exporter `raw_codes[:20]` 实只输出 3 行（bug 在基线中，需修复） | `baseline/evidence/*`；`validate_fixtures.py` PASS_WITH_DOCUMENTED_LEGACY_EXPORT_DEFECT；`scripts/export_selection_audit.py:121-135`（本人复核） | 最终 exporter 必须输出显式 `raw-top3` + 真 20 行 `raw-top20`；parity 一律用 reconstructed |
| 4 | 源码地图：`ui/tushare_v1_session.py` 2,273 LOC；`runtime/tushare_runtime.py` 1,045（TushareV1Runtime/ScanOutcome）；`storage/sqlite.py` 1,310（SQLiteStore，schema v1-v6 迁移器）；`engine/*`（candidates 707、stable_top3、alerts 276、sector、scheduler）；`providers/tushare/*`（transport/router/capabilities）；`security/credential_store.py`（Keychain） | `wc -l src/stock_watcher/**/*.py` 实测；grep 类/方法清单 | 复用 engine/runtime/providers/storage；只抽服务不复制规则 |
| 5 | 可复用与必须替换：复用 domain/engine/runtime/automation/scan_coordinator/data_health/market_session/universe_cache/post_close_*；替换 macOS Keychain→AES-256-GCM+Docker secret；LaunchAgent→compose restart policy；PySide6 窗口→Jinja2+原生 CSS/ES modules；系统弹窗→WS+Notification API；单实例锁→SQLite lease/fencing | `04-Core-Reuse-and-Refactor-Map.md`；源码勘察 | 共享服务依赖方向 `server/ui -> services -> runtime/engine/providers/storage/domain`，Mac UI 后续逐步适配，本版本不动桌面类 |
| 6 | 依赖阻塞：`pyproject.toml` 通用依赖含 `PySide6`、`pyobjc-framework-cocoa`、`keyring`；Python 3.12 可用（`.venv` 3.12.11，uv 0.9.26） | `pyproject.toml` 实测 | 拆四组：core/headless（numpy/pydantic/PyYAML/reportlab/requests/tushare/tzdata）、desktop-macos（keyring/pyobjc/PySide6）、server（fastapi/uvicorn/jinja2/argon2-cffi/cryptography）、dev/test；重新生成 uv.lock；Linux no-Qt import gate 测试 |
| 7 | 业务参数：Candidate（5.0/2.0/2.0/7.0/50.0/32.0/同板块2/显示3/排除规则/补位语义）、sector gate（>=3/>0/>0.50/>=3/rank<=min(5,30%)/行业 tie margin 1.0）、Stable Top3（8.0/3 周期/45s/失格即退/延迟替换但刷新价格）、alert（100/0.8/0.35/18/0.1/0.2/冷却5min/日限3/替换1.0/2周期）、health（0.99/60s/60s/10s/60s/120s/冷1轮/恢复3轮）、时段 09:30-11:30/13:00-15:00 Asia/Shanghai | `03-Functional-Parity-Matrix.md`；engine 源码（AlertPolicyConfig、StrongMovementDetector、StableTop3Selector、DataHealthConfig 实测参数一致） | Web 化不改任何参数；参数由 fixture parity + 参数锁定测试守护 |
| 8 | 扫描/健康：原子全市场 snapshot、重叠锁（FullMarketScanCoordinator）、coverage>=0.99、source span<=60s、quote age<=60s、future skew<=10s、fresh<=60s/stop>120s、冷启动 1 健康轮、恢复 3 fresh 轮 | `runtime/scan_coordinator.py`、`runtime/data_health.py` 实测 | Worker 唯一调用 `TushareV1Runtime.scan_once()`；不达标不更新正式 Top3 |
| 9 | 自动任务/PDF：09:45/14:45 deadline 90s；15:30 deadline 8h；durable planned/running/succeeded/failed；local-first fallback 仍产 JSON/MD/PDF；local Top3 来源 = 成功 14:45 payload -> 15:00 前最后健康 stable batch -> 最后提醒；禁止 closing_performance 冒充；PDF manifest 含 mode/renderer/source version/source SHA/source commit | `AutomationPlanner`、`_execute_summary_task`/`_generate_summary`（session 1077-1250 行）实测 | 服务层实现相同语义，manifest 由 `write_pdf_manifest`/`manifest_is_current` 保证 |
| 10 | 数据源路线：普通/static 走 `TushareSdkProTransport`（`https://fastapic.stockai888.top`）；realtime 走 `NativeRealtimeTransport`（`tushare.realtime_quote(src="sina")`，校验 `https://realtime.stockai888.top`）；同 Token；TdxQuant 仅高级诊断 | `config/data_sources.py`、`_runtime_factory` 实测；本机 curl 两 host 均可达（HEAD 405=TLS/路由正常） | VPS 必须重新 preflight（1/100/300/800/full），Mac 结果不外推；无 VPS/Token 时标记 pending |
| 11 | 服务抽取方案：新建 `services/stockwatcher_service.py`（headless `StockWatcherService`），显式注入 clock/store/credential provider/event sink/report dir/source commit；依赖方向不反向；Worker 与 Mac UI 共用同一 engine/runtime/storage，业务规则只有一份；防分叉 = fixture parity 门 + no-Qt import gate | `04-Core-Reuse-and-Refactor-Map.md` | 抽取循环：任务准备→扫描→记录→提醒→总结→公共状态/outbox；桌面类保持不变，其 349 测试继续通过 |
| 12 | 进程所有权：唯一 Worker 持有 runtime/buffer/selector/detector/policy/automation；Web 只读 + 命令/事件；单 Uvicorn worker；lease 名 `stockwatcher-worker`、heartbeat 5s、TTL 20s、BEGIN IMMEDIATE 获取、fencing token、丢失即停；`--scale worker=2` 第二个必须退出 | `05-Worker-Scheduler-and-Command-Model.md`；007 SQL `service_leases` | 实现 LeaseManager + duplicate-worker 测试 |
| 13 | schema v7：新增 web_users/web_sessions/web_user_state/service_leases/secret_requests/encrypted_secrets/web_commands/web_events/web_public_state/web_audit_log；v6 增量迁移进 Python runner；迁移前 backup API；BEGIN IMMEDIATE 事务；迁移后 integrity/FK/必需表断言；回滚=迁移前备份 | `06-Database-Migration-v7.md`；`database/007_web_internal_test.sql`（`validate_reference_migration.py` 对真实 v6 库验证通过：schema 7、integrity ok、0 FK 失败、sentinel 保留） | 将 007 SQL 语义并入 `SQLiteStore._apply_v7_migration`；CURRENT_SCHEMA_VERSION=7 |
| 14 | API/WS：OpenAPI 23 路径（auth/state/candidates/history/alerts/summaries+pdf/commands/admin）；detail 绑定 `snapshot_id+code`，旧 snapshot 409 `snapshot_changed`；WS `/ws/v1/events?after_id=`，hello/snapshot/heartbeat/补发/resync_required/有界队列/慢客户端 1013；事件含 schema_version/event_id/occurred_at/source_commit/correlation_id；payload 无秘密 | `contracts/openapi.yaml`、`contracts/websocket-events.schema.json`（meta 校验通过） | FastAPI 按契约实现；WS 不是事实源，REST 可恢复 |
| 15 | UI/通知边界：登录/Dashboard/Alert Center/History/Daily Summary/Admin 六页；Jinja2+原生 CSS+ES modules，无 CDN/Node；共享同一业务状态，权限只控可见性；浏览器打开/后台实时，关闭后事件落库下次可见；第一阶段不做 Web Push | `09-Web-UI-Spec.md` | 页面实现 + WCAG AA/360px/键盘可达/明确状态；Notification 由用户手势触发 |
| 16 | 认证/Secret：Argon2id（m=65536KiB,t=3,p=2 基线，配置校准）；首 Admin 仅 CLI+stdin；opaque session cookie（Secure/HttpOnly/SameSite=Lax，DB 只存 SHA-256）；CSRF 独立值+Origin 校验；登录/命令限速；Token AES-256-GCM+AAD（secret name/key version/env），master key 为 VPS 本地 Docker secret；先测后激活失败保旧；全局 redaction；不出现于 HTML/JS/API/WS/日志/镜像/交付包 | `10-Authentication-Secrets-and-Security.md` | 实现 auth/secret/redaction + 全链路 secret scan 测试 |
| 17 | Docker/运维/验收：同一镜像不同命令（web/worker）；CLI migrate/create-user/backup/restore/healthcheck；Caddy 唯一公网入口+安全头；持久卷 state/reports/backups；SQLite backup API；升级 backup→build→migrate→switch→smoke；回滚保证据不删库；测试门=原测试+Ruff+Mypy strict+no-Qt gate+fixture parity+服务/并发/安全/E2E/双 worker/恢复演练+18 条交易日验收 | `11/12/13` 文档；deploy 模板；Docker 29.2.1（守护进程已启动） | 实现全部入口与脚本；Docker 侧证据在本地构建；VPS Live 标记 pending |
| 18 | 实施起点：分支 `web/internal-test-v1-handoff`@502a447；首批提交顺序=依赖拆分→exporter 修复→schema v7→services→server/worker/CLI→UI→deploy→tests→evidence；硬阻塞=域名/VPS/Token/master key（占位符，本地代码/测试/容器继续完成） | 本文件 + `git log --oneline` 将展示提交序列 | 立即进入依赖拆分与实现，不在方案阶段停留 |

## 关键事实声明

- legacy `raw-top20` 实为 Raw Top3（每轮 3 行，`audit.raw_codes`）；真实 Top20 使用 `raw-top20-reconstructed`（每轮 20 行）。
- 基线 `scripts/export_selection_audit.py` 第 3 节仍写 `raw_codes[:20]`（实得 3 行）——最终 exporter 必须修复并补 `raw-top3` 显式导出。
- 最终 `502a447` 尚无完整交易日 Live 证据；Mac 结果不充当 Windows/VPS 证据；VPS preflight 与 18 条交易日验收在取得 VPS/Token 前标记 pending，不伪造。
